/**
 * Server-side access to carried shulker boxes through the standard Fabric
 * Transfer API. Callers own discovery, ordering, batching, and transactions.
 */
package net.kyrptonaught.quickshulker.api.shulker.server;
