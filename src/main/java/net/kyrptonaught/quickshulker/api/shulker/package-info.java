/**
 * Fixed-size value objects for Quick Shulker's screen-independent client
 * protocol.
 *
 * <p>The protocol deliberately addresses one player inventory slot and one
 * slot in one carried shulker. It does not expose server searches, container
 * snapshots, transactions, or caches.</p>
 */
package net.kyrptonaught.quickshulker.api.shulker;
